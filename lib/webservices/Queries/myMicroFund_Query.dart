import '../../webservices/Clients/AppClients.dart';

/*
void Create_myMicroFundQuery(String token) {

  final meQuery_Reg = GmyMicroFund_QueryReq();
  final client = initClientWithToken(token);

  client.request(meQuery_Reg).listen((response) {
    final result = response.data;
    print(response.hasErrors);
    print(response.linkException.originalException.toString());
  }).onError((handleError) {
    print(handleError.toString());
  });
}*/
